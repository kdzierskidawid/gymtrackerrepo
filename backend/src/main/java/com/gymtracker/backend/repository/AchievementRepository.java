package com.gymtracker.backend.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AchievementRepository {
    // This repository is no longer used and can be removed.
}
